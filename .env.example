# Database Configuration
POSTGRES_DB=simpleblog
POSTGRES_USER=simpleblog_user
POSTGRES_PASSWORD=simpleblog_dev_password_123

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@simpleblog.local
PGADMIN_DEFAULT_PASSWORD=admin

# Application Configuration
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:5000

# JWT Configuration (Development only - use strong secret in production)
JWT_KEY=SimpleBlog_Secret_Key_For_Dev_1234567890_MIN_32_CHARS_REQUIRED
JWT_ISSUER=SimpleBlog
JWT_AUDIENCE=SimpleBlog

# Email Configuration (Development - use SMTP server details in production)
SMTP_SERVER=localhost
SMTP_PORT=587
SMTP_SENDER_EMAIL=noreply@simpleblog.local
SMTP_SENDER_NAME=SimpleBlog
SMTP_USE_SSL=false
